Update the $Prefix = "contoso" to relevant environment and execute the script CreateCert
Confirm certs are generated in the folder

Check and remove any exisiting certs with the same name and remove them in both current machine and current user.
Navigate in all the subfolders and confirm

Execute Import certs on all the servers